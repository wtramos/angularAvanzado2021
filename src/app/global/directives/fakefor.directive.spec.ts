import { FakeforDirective } from './fakefor.directive';

describe('FakeforDirective', () => {
  it('should create an instance', () => {
    const directive = new FakeforDirective();
    expect(directive).toBeTruthy();
  });
});
