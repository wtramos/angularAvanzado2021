import { FakeifDirective } from './fakeif.directive';

describe('FakeifDirective', () => {
  it('should create an instance', () => {
    const directive = new FakeifDirective();
    expect(directive).toBeTruthy();
  });
});
